{
  "name": "receipt-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm ci --prefix editor && vite build editor --base=/editor/ --outDir ../dist/editor --emptyOutDir false",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  },
  "dependencies": {
    "gifuct-js": "^2.1.2",
    "three": "^0.186.0"
  }
}
